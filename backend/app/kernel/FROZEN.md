# The kernel is frozen

- A kernel change needs an ADR carrying a deletion-test argument.
- A seam needs two named adapters; one adapter is a hypothetical seam, not a port.
- The ledger's writer role is never widened.
- RLS keys on `org_id` only.
- The twelve locks live in the vault's ADRs 0001-0012.
