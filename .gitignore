# Claude Code — session-local machinery, never committed
.claude/.curate/
.claude/settings.local.json

# OS noise
.DS_Store
.gstack/

# factory branch: the factory files are tracked here; main keeps them ignored
__pycache__/
