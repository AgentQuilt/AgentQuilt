# Claude Code — session-local machinery, never committed
.claude/.curate/
.claude/settings.local.json

# local secrets
.env*

# OS noise
.DS_Store
.gstack/

__pycache__/

# review prompts and outputs (codex-review)
temp/

# Obsidian workspace
.obsidian/

# wave worktrees (each is its own checkout, never a gitlink here)
.claude/worktrees/

# Lavish review artifacts and session state (local review surface, never committed)
.lavish/
